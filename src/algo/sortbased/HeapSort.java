package algo.sortbased;

/**
 * Elements are inserted into Min Heap or Max Heap (Priority Queue). Then elements are removed from the top.
 *
 * Time complexity = O(n log n)
 *
 */
public class HeapSort {
}
